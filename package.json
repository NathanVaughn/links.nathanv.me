{
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "cross-spawn": "^7.0.5",
    "hugo-extended": "^0.161.0"
  },
  "scripts": {
    "build": "node build.js",
    "install": "git submodule update --init --recursive",
    "server": "hugo server"
  },
  "allowScripts": {
    "hugo-extended": true
  }
}
